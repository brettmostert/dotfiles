#!/bin/bash
#Generated by JetBrains Toolbox 2.9.1.58121 at 2025-10-03T17:48:42

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/brettmostert/Applications/GoLand.app/Contents/MacOS/goland" $wait --args "${intellij_args[@]}"